{
  "name": "leap-app",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "cd backend && bun run dev",
    "install:all": "bun install && cd backend && bun install && cd ../frontend && bun install"
  }
}